<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AntoKeberApps">

        <!--  PERTEMUAN 5 -->
        <activity
            android:name=".pertemuan_5.FifthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- WebView Activity -->
        <activity
            android:name=".pertemuan_5.WebViewActivity"
            android:exported="false" />

        <!-- Pertemuan 4 -->
        <activity android:name=".pertemuan_4.Custom_2Activity" android:exported="false" />
        <activity android:name=".pertemuan_4.Custom_1Activity" android:exported="false" />
        <activity android:name=".pertemuan_4.BRuangActivity" android:exported="false" />
        <activity android:name=".pertemuan_4.DashboardActivity" android:exported="false" />

        <!-- Pertemuan 3 -->
        <activity android:name=".pertemuan_3.ThirdResultActivity" android:exported="false" />
        <activity android:name=".pertemuan_3.ThrdActivity" android:exported="false" />

        <!-- Main -->
        <activity android:name=".MainActivity" android:exported="false" />

    </application>

</manifest>